version: 2

# Try using these source definitions instead!
sources:
  - name: parish_interest_survey
    schema: parish_interest_survey_portfolio
    description: Raw response data from the parish ministry interest survey.
    tables: 
      - name: anonymized_parish_interest_survey_raw
        description: One record per response to the survey.